<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.nuritelecom.mapper.BoardMapper">

	<insert id="create" parameterType="com.nuritelecom.domain.BoardVO">
		INSERT INTO tbl_board(title, content, writer) VALUES (#{title}, #{content}, #{writer});
	</insert>

	<select id="read" resultType="com.nuritelecom.domain.BoardVO">
		select * from tbl_board where bno = #{bno}
	</select>

	<update id="update" parameterType="com.nuritelecom.domain.BoardVO">
		update tbl_board set title= #{title}, content=#{content}, writer = #{writer} where bno = #{bno}
	</update>

	<delete id="delete">
		delete from tbl_board where bno = #{bno}
	</delete>
	
	<select id="listAll" parameterType="int" resultType="com.nuritelecom.domain.BoardVO">
		<![CDATA[
			select * from tbl_board where bno>0
			order by bno desc, regdate desc limit #{page*20} , 20
		]]>
		
	</select>
	
	<select id="total" resultType="int">
		select count(*) from tbl_board
	</select>

</mapper>